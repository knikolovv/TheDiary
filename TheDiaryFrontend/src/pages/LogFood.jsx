import FoodEntry from "../components/FoodEntry";

export default function LogFood() {

    const handleCreate = async (mealEntry) => {
        try {
            
        } catch (error) {
            
        }
    }
    
    return (
       <FoodEntry></FoodEntry>
    );
}